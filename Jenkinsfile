pipeline {
    agent any
    stages {
        stage('compile') {
            steps {
                sh "mvn compile"
            }
        }
        stage('compile') {
            steps {
                sh "mvn test"
            }
        }
        stage('compile') {
            steps {
                sh "mvn package"
            }
        }
    }
}
